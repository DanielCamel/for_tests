name: CI Pipeline

on:
  push:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Build
        run: |
          echo "🚀 Начало сборки..."
          echo "Коммит: ${{ github.sha }}"
          echo "Ветка: ${{ github.ref }}"
          
          # Обычные команды сборки
          echo "📦 Установка зависимостей..."
          # ваши команды...
          
          echo "⚡ Сборка проекта..."
          # ваши команды...
          
          echo "✅ Сборка завершена"
